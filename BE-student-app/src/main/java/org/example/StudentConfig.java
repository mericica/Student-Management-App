package org.example;
/*
import org.example.repository.GradeRepository;
import org.example.repository.RoleDbRepository;
import org.example.repository.UserDBRepository;
import org.example.service.UserDbServiceImpl;
import org.example.service.UserService;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.jpa.repository.config.EnableJpaRepositories;
import org.springframework.security.authentication.AuthenticationManager;

@Configuration
@EnableJpaRepositories
public class StudentConfig {
    @Bean
    public UserService userDbService(UserDBRepository userDBRepository, RoleDbRepository roleDbRepository, AuthenticationManager authenticationManager, GradeRepository gradeRepository) {
        return new UserDbServiceImpl(userDBRepository, roleDbRepository, authenticationManager, gradeRepository);
    }
}

 */